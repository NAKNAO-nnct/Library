const conf = {
  LOCALSTORAGE_BOOKLIST_KEY:
    process.env.LOCALSTORAGE_BOOKLIST_KEY ?? 'BookListKey',
  LOCALSTORAGE_FOLDERLIST_KEY:
    process.env.LOCALSTORAGE_FOLDERLIST_KEY ?? 'FolderListKey',
}

export default conf
